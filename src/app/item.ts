export interface Item {
  pound: number;
  product: string;
  price: number;
  quantity: number;
  edit: string;
}

// Above - edit is supposed to be boolean, how to make that appear in hard coded array on products component ts?
